package cx.cad

/**
  * This library provides a way to score games of ten-pin bowling using the
  * [[https://en.wikipedia.org/wiki/Ten-pin_bowling#Traditional_scoring traditional scoring method]].
  *
  * @example
  *          {{{
  *          val rolls = StdIn.readLine().split(' ').map(_.toInt)
  *          val score = Game.from(rolls).score
  *          }}}
  */
package object bowling {

}
